name: custom-on-push

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3.6.0
      - run: npm ci 
      - run: make lint
      - run: make test
  